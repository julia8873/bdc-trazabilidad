# Fichero de orquestación de producción
# Este fichero constituye la única costura de infraestructura entre
# bdc-trazabilidad y llm-wiki-assistant.
# Ver docs/integracion-bdc.md para detalles y procedimiento de reversión.

include:
  - path: ../../llm-wiki-assistant/moodle-matrix-dev/docker-compose.yml
  - path: ../docker-compose.yml
