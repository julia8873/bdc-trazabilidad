[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "metrics-api"
version = "0.1.0"
description = "API de métricas"
dependencies = [
    # TODO (Fase 3): Al implementar la dependencia real, fijar la versión exacta 
    # (ej. "shared-pkg==1.0.0") para cumplir con la Regla 3 del plan maestro.
    "sqlalchemy>=2.0",
    "alembic>=1.13",
    "psycopg2-binary>=2.9",
    "pytest>=8.0",
    "requests>=2.31",
    "responses>=0.25",
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.28.0",
    "pydantic>=2.0",
    "httpx>=0.25.0",
    "PyJWT>=2.8.0"
]
